'use strict';
/*
var myApp = angular.module('AngularSampleBhoomiApp', ['ngResource'])
  .config(function ($routeProvider) {
    $routeProvider
      .when('/', {
        templateUrl: 'views/main.html'
      })
      .when('/new/:Id', {
            controller:'newController',
            templateUrl:'views/SalesEdit.html',
            resolve: {
                sale: function(getSale) {
                    return getSale();
                }
        }})
      .when('/edit/:Id', {
            controller:'editController',
            templateUrl:'views/SalesEdit.html',
            resolve: {
                sale: function(getSale) {
                    return getSale();
                }
            }})
      .when('/list', {
            controller:'salesListController',
            resolve: {
                sales: function(getAllSales) {
                    return getAllSales();
                }
            },
            templateUrl:'views/saleslist.html'})
      .otherwise({
        redirectTo: '/'
      });
  });
*/
