'use strict';

/*
describe('Filter: salesFilter', function () {

  // load the filter's module
  beforeEach(module('AngularSampleBhoomiApp'));

  // initialize a new instance of the filter before each test
  var salesFilter;
  beforeEach(inject(function ($filter) {
    salesFilter = $filter('salesFilter');
  }));

  it('should return the input prefixed with "salesFilter filter:"', function () {
    var text = 'angularjs';
    expect(salesFilter(text)).toBe('salesFilter filter: ' + text);
  });

});
*/